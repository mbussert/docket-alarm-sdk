import { Base } from "./base";

export class Methods extends Base {
  async docketAlarmSearch() {
    // Requires: login_token, q, o, limit, offset
    // Optional: client_matter, scroll / scroll_parallel / scroll_index
  }
}
